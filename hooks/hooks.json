{
  "description": "SessionStart check that an authoritative AgentCore documentation source is available — the first-party amazon-bedrock skill, or failing that an AWS documentation MCP. Silent when one is present; never blocks a session.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-aws-agents-plugin.sh\" 2>/dev/null || true",
            "timeout": 10,
            "statusMessage": "Checking AgentCore documentation source availability"
          }
        ]
      }
    ]
  }
}
